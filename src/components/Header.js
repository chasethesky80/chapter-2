import "./Header.css"
import Link from './Link'

export default function Header({ logo }) {
    return (
        <header className="App-header">
          <h1>My Portfolio</h1>
        </header>
    )
}